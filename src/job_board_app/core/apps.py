"""
App configuration for "core" app job_board_app project.
"""

from django.apps import AppConfig


class CoreConfig(AppConfig):
    """App config for Core app."""

    default_auto_field = 'django.db.models.BigAutoField'
    name = 'core'
